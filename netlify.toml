[build]
  base = "frontend"
  command = "npm run build -- --configuration production"
  publish = "dist/certificate-frontend/browser"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
