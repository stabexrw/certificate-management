[build]
  command = "cd frontend && npm ci && npm run build -- --configuration production"
  publish = "frontend/dist/certificate-frontend/browser"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
